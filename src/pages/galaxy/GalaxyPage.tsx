// GalaxyPage.tsx
import * as React from 'react';

const GalaxyPage: React.FC = () => {
    return (
        <div>
            <h1>Galaxy Page</h1>
            <p>This is the text that will be displayed on the Galaxy page.</p>
        </div>
    );
};

export default GalaxyPage;
